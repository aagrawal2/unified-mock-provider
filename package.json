{
  "name": "root",
  "version": "1.0.0",
  "description": "this file is only required in dev mode when we want to run nodemon with babel-register",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-register": "^6.26.0"
  }
}
